import React from "react";

export default function Menu() {
     return (
          <div className="flex items-center gap-x-5 text-base font-semibold text-[#fff]">
               <span>Add Product</span>
               <span>Update Product</span>
               <span>Delete Product</span>
          </div>
     );
}
